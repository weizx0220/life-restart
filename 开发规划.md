# 《浮生若梦》接手开发规划

> 结论先行：**根目录 = 源码仓库（唯一事实源），`_deploy` = 纯部署产物（从源码生成，可丢弃）。**
> 开发→校验→预览→打包→部署，五步走，工具链用现成的 git + node + python，不引入任何构建框架。

---

## 一、现状盘点（全部实测，非估计）

| 项目 | 事实 |
|---|---|
| 源码位置 | 根目录 `life-restart/`：index.html + js/ + css/ + assets/，**无 git** |
| 部署仓库 | `_deploy/`（有 git，remote=weizx0220/_deploy，GitHub Desktop 在管，main 分支干净同步） |
| 代码一致性 | 根目录 js/css 与 _deploy **完全一致**；唯一差异：根目录多 3 个社交死代码文件 |
| index.html | 根目录是**干净的**；`_deploy/index.html` 被编辑器注入了几百个 `data-page-node-id="xxx"` 垃圾属性（污染版） |
| 社交系统 | `js/social.js` + `js/data/npcs.js` + `js/data/events_social.js` + `assets/chahua/np_*.jpg`×8 —— 8-19 主动回退后遗留，**未进 _deploy** |
| 素材体积 | 总 1.3G。其中 **4 个 `jimeng-*` 原始批次 = 1.18G（1178M）**，全是未精选的即梦原图 |
| 核心素材 | chahua 精选插画 36M（164 张）+ audio 42M + ui 44K ≈ **76M**，无 >20M 单文件 |
| 中间产物 | `_sheets/` `_sheets2/` `chahua/_originals/` `audio/备选/` ≈ 35M，非运行必需 |
| 开发脚手架 | `test/`（44 个脚本 + node_modules 29M），依赖 puppeteer-core，已有 balance.js / e2e.js / build_single.py / deploy_github.sh |
| 构建脚本 | `test/build_single.py`：css/js 内联进 index.html → 生成「浮生若梦-单文件版.html」，可用 |
| 无 | 根目录和 _deploy 都**没有 .gitignore** |

**关键判断**：素材真正要进版本库的只有 ~76M 核心素材，1.18G 的即梦原图全部该忽略——这直接决定了 A 方案可行，不用上 Git LFS。

---

## 二、目标架构

```
life-restart/            ← git 仓库（源码，唯一事实源）
├── index.html
├── js/  css/  assets/   ← 核心素材（~76M 进 git）
├── test/                ← 开发脚手架（脚本保留，node_modules 忽略）
├── tools/               ← 新增：校验/打包/部署脚本统一放这
├── .gitignore
└── docs/                ← 新增：文档

_deploy/                 ← 不纳入 git（本地保留，作为部署中转，或彻底废弃改走 Actions）
```

**部署链路（推荐）**：`push 源码 → GitHub Actions 自动 build + 部署 Pages`，`_deploy` 目录退役。

---

## 三、标准开发流程（改一行代码的完整闭环）

1. **改**：只改根目录源码，永不手改 `_deploy`。
2. **校验**（改完必跑，30 秒）：
   - `node --check` 逐个校验改动的 js 语法（_schema.md 硬性要求）
   - 数据一致性校验脚本（见 P1-①）
3. **预览**：`python -m http.server`（不用 `file://`，避免 localStorage/音频在本地协议的坑）。
4. **打包**：`python tools/build_single.py` 生成单文件版（改完发布前才需要）。
5. **提交**：`git add` 只加源码 + 核心素材，`git commit` 写清楚改了啥。
6. **部署**：push 到 main，Actions 自动出包上线（或手动同步 `_deploy`，见决策 3）。

---

## 四、工具链（全现成，零新增依赖）

| 工具 | 用途 | 状态 |
|---|---|---|
| git | 版本控制 | 已装 2.55.0，全局用户已配 |
| node | js 语法校验、数据校验、e2e | 已装（managed 22.22.2） |
| python | 单文件打包（build_single.py） | 已装（3.13.12） |
| puppeteer-core | 冒烟测试/截图（test/ 已有） | 已装（test/node_modules） |

**明确不引入**：webpack / vite / esbuild / TypeScript。项目既定约束是「无构建、`<script>` 顺序加载、纯 var 全局」，上构建工具会破坏这套简单约定、收益几乎为零、还得重写 35 个 js 文件的引用关系。这是过度设计，否决。

---

## 五、基础架构提升（按优先级，每个标注收益与理由）

### P0 —— git init 前必须定，否则会埋雷

**① `.gitignore`**（最高优先，1 分钟的事但影响整个仓库健康度）

```gitignore
# 部署产物（从源码生成）
_deploy/
浮生若梦-单文件版.html

# 素材中间产物 / 原始生图批次（1.2G，非运行必需）
assets/jimeng-*/
assets/_sheets/
assets/_sheets2/
assets/chahua/_originals/
assets/audio/备选/

# 开发脚手架依赖
test/node_modules/

# 系统/编辑器垃圾
.DS_Store
Thumbs.db
*.log
```

### P1 —— 高价值、低风险，接手后第一批做

**① 数据一致性校验脚本**（`tools/validate.js`，node 零依赖）
校验 15 个 data 文件的：id 全表唯一性；事件/世界/副本/遗物引用的 `flag / item / skill / card / relic / ending / talent` id 是否真实存在；cond 结构是否合法；结局/成就依赖的 flag 是否被某事件真正设置。
**理由**：这游戏是纯数据驱动，事件池近 800KB、几百个 flag 交叉引用，是最容易「改了 A 漏了 B」的地方。现在只有 agecheck/badgecheck/cornercheck 几个散装脚本，缺一个统一校验器。跑一次就能挡住「引用不存在 id → 运行时静默失效」这类隐性 bug。

**② 语法校验**：把 `node --check` 串进校验脚本，一键跑完所有 js。

**③ 本地开发服务器约定**：统一 `python -m http.server` 预览，写进 README/文档。

### P2 —— 有价值，稳定后做

**④ 一键构建部署**：整合「单文件打包 + 同步 _deploy + push」成一个命令。现成的 `deploy_github.sh` 有两个 bug 要修：`REPO` 写死了 `life-restart`、还 `rm -rf _deploy/.git` 破坏 GitHub Desktop 的仓库。改造为 `tools/deploy.sh`。

**⑤ GitHub Actions 自动部署**：源码 push → 自动 build 单文件版 → 部署 Pages。收益最大（从此不用手动同步），但要你在仓库里开 Pages + 配 Actions 权限，需要你配合。

**⑥ e2e 冒烟测试**：复用 test/e2e.js + puppeteer，自动跑「开局→抽签→分配→推进 N 年→死亡→结算」核心链路，防止改动让游戏白屏/卡死。

### P3 —— 暂不做（避免过度设计）

- **事件从 js 迁 JSON**：会丢失 `text: function(L)` 动态文本能力，否决。
- **模块化/打包器**：见第四节，否决。
- **事件文件拆分/懒加载**：events_reality 350KB 单文件确实大，但拆分会破坏「顺序加载」简单约定，且纯文本不占多少内存，收益低。

---

## 六、Git 方案细节

1. 根目录 `git init`，branch `main`。
2. 核心素材（chahua 精选 + audio + ui）进 git；1.2G 即梦原图 ignore（`.gitignore` 已列）。
3. 初始提交 = 当前干净源码（不含 `_deploy`、不含社交死代码——见决策 1）。
4. 换行符：设 `core.autocrlf`，避免 index.html CRLF/LF 抖动。
5. 提交规范：`fix: / feat: / balance: / art: / chore:` 前缀，一句话说清。

---

## 七、下午回来第一步（执行清单，按序，每步可验证）

1. 拍板第八节 3 个决策（不拍板我按推荐默认走，并在改前复述）。
2. 我写 `.gitignore` + `tools/validate.js`，跑通校验（预期：暴露社交系统引用的 np_* 缺失等历史遗留，作为整改清单）。
3. `git init` + 首次提交干净源码。
4. 根据决策 3 接部署链路（Actions 或手动脚本）。
5. 出「遗留问题清单」，从 P1 校验暴露的 bug 开始逐个修。

---

## 八、需要你拍板的 3 个决策

| # | 问题 | 选项 | 我的推荐 |
|---|---|---|---|
| 1 | 社交系统死代码（3 js + 8 头像）怎么处理 | a) 彻底删除 b) 本地保留不进 git c) 恢复启用 | **a 彻底删除**——既然 8-19 主动回退了，留着只会让校验报引用缺失、让新人误判 |
| 2 | 源码仓库叫什么 | a) 新建 `life-restart` b) 复用 `_deploy` c) 只本地 git 暂不建远程 | **a 新建 life-restart**（`_deploy` 名字是「部署」，语义混乱） |
| 3 | 部署方式 | a) GitHub Actions 自动 b) 手动同步 _deploy | **a Actions 自动**，一劳永逸；你不熟的话我全程带 |
